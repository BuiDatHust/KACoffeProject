<%- include('../_layouts/header') ; -%>
    <link rel="stylesheet" href="sweetalert2.min.css">
    <section class="hero-wrap hero-wrap-2" style="background-image: url('/images/bg_5.jpg');" data-stellar-background-ratio="0.5">
        <div class="overlay"></div>
        <div class="container">
            <div class="row no-gutters slider-text align-items-center justify-content-center">
                <div class="col-md-9 ftco-animate text-center">
                    <h1 class="mb-2 bread">Profile</h1>
                    <p class="breadcrumbs"><span class="mr-2"><a href="index.html">Trang chủ <i class="ion-ios-arrow-forward"></i></a></span> <span>Quản lý cửa hàng <i class="ion-ios-arrow-forward"></i></span></p>
                </div>
            </div>
        </div>
    </section>

    <section class="py-5 my-5">
        <div class="container">
            <h1 class="mb-5">Quản lý cửa hàng</h1>
            <div class="bg-white shadow rounded-lg d-block d-sm-flex">
                <div class="profile-tab-nav border-right">
                    <div class="p-4">
                        <div class="img-circle text-center mb-3">
                            <img src="/images/avt.jpg" alt="Image" class="shadow">
                        </div>
                        <h4 class="text-center">Admin</h4>
                    </div>
                    <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical">
                        <a class="nav-link active" id="products-tab" href="/KACoffe/v1/admin"> Sản phẩm
                        </a>
                        <a class="nav-link" id="discount-tab" href="/KACoffe/v1/admin/discount">
                            Mã giảm giá
                        </a>
                        <a class="nav-link" id="user-tab" href="/KACoffe/v1/admin/user">
                            Người dùng
                        </a>
                        <a class="nav-link" id="order-tab" href="/KACoffe/v1/admin/order">
                            Đơn hàng
                        </a>
                        <a class="nav-link" id="stories-tab" href="/KACoffe/v1/admin/stories">
                            Tin tức
                        </a>
                        <a class="nav-link" id="statistic-tab" href="/KACoffe/v1/admin/statistic">
                            Thống kê
                        </a>
                    </div>
                </div>
                <div class="tab-content p-4 p-md-5" id="v-pills-tabContent">
                    <div class="tab-pane fade show active" id="products" role="tabpanel" aria-labelledby="account-tab">
                        <div class="grid__column-10">
                            <div class="cart-cart">
                                <span class="subheading" style="font-size:28px;">Danh sách sản phẩm</span>
                                <table class="cart-table">
                                    <tr class="cart-bottom">
                                        <td colspan="6">
                                            <a href="/KACoffe/v1/admin/createProduct" class="cart-bottom-btn">Thêm sản phẩm</a>
                                        </td>
                                    </tr>
                                    <tr>
                                        <!--<th style="width: 30px; margin-left: 8px;"><span class="cart-all">Tất cả</span></th>-->
                                        <th class="admin-header__products">Sản phẩm</th>
                                        <th class="admin-header__price">Giá</th>
                                        <th class="admin-header__header">Thao tác</th>
                                    </tr>
                                    <% products.forEach(function(product){  %>
                                    <tr class="cart-item">
                                        <!--<td class="button__check"><input type="checkbox" checked="checked" class="cart-check" /></td>-->
                                        <td class="cart-txt-left">
                                            <img src="<%= product.Image[0] %>" alt="" class="cart-txt-left-img">
                                            <a href="/KACoffe/v1/menu/<%= product._id %>" class="cart-name">
                                                <%= product.name  %>
                                            </a>
                                        </td>
                                        <td><span class="cart-price"><%= product.price  %></span></td>
                                        <td>
                                            <form id="product<%= product._id %>" action="/KACoffe/v1/admin/allproduct/delete/<%= product._id  %>" method="post">
                                                <a class="mr-1" href="/KACoffe/v1/admin/allproduct/<%= product._id  %>">
                                                    <button id="sua" type="button">
                                                        <i class="fas fa-pencil-alt"></i>
                                                    </button>
                                                </a>
                                                <button id="xoa" onclick="event.preventDefault();DeleteProduct('<%= product._id %>');">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                    <% }) ; %>
                                </table>
                                <br>
                                <% if ( pages > 1) { %> 
                                <nav aria-label="...">
                                    <ul class="pagination">
                                        <% if ( page == 1) { %> 
                                            <li class="page-item disabled">
                                                <a class="page-link" href="#">Previous</a>
                                            </li>
                                        <% } else { %>
                                            <li class="page-item">
                                                <a class="page-link" href="?page=<%= page - 1 %>">Previous</a>
                                            </li>
                                        <% } %>
                                        <% for( let i=0; i < pages; i++ ){  %>
                                            <% if ( i == page - 1) { %> 
                                                <li class="page-item active">
                                                    <a class="page-link" href="?page=<%= i + 1 %>"><%= i + 1 %></a>
                                                </li>
                                            <% } else { %>
                                                <li class="page-item">
                                                    <a class="page-link" href="?page=<%= i + 1 %>"><%= i + 1 %></a>
                                                </li>
                                            <% } %>
                                        <%  } %>
                                        <% if ( page == pages) { %> 
                                            <li class="page-item disabled">
                                                <a class="page-link" href="#">Next</a>
                                            </li>
                                        <% } else { %>
                                            <li class="page-item">
                                                <a class="page-link" href="?page=<%= page + 1 %>">Next</a>
                                            </li>
                                        <% } %>
                                    </ul>
                                </nav>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
<%- include('../_layouts/footer') ; -%>

<script>
    async function DeleteProduct(productId) {
        const productDel = document.getElementById(`product${productId}`)
        await Swal.fire({
                title: 'Bạn có chắc xóa?',
                text: "Bạn không thể quay lại kết quả",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Có !'
            })
            .then((result) => {
                if (result.isConfirmed) {
                    Swal.fire(
                        'Thành công!',
                        'Xóa thành công',
                        'success'
                    )
                    .then(() => {
                        productDel.submit()
                    })
                }
            })
        }
</script>
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>